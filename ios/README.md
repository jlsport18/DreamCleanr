# DreamCleanr iOS Viewer

Planned home for the SwiftUI iPhone/iPad viewer-controller.

The iOS app should:

- render the same DreamCleanr receipt and summary model used on macOS
- act as a viewer or remote trigger surface only
- avoid any local cleanup logic that belongs in the Python core
